# Godot 4 — import önbelleği ve geçici dosyalar
.godot/
*.import

# Hata ayıklama görüntüsü (oyun --capture ile üretir)
capture.png

# Export çıktıları (DMG vb. — büyük binary, repoya girmez)
build/
export_templates/

# Yedekler / OS
*.bak
*.bak*
Thumbs.db
.DS_Store
